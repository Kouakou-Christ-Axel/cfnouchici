import { beforeEach } from "vitest";
import { db } from "@/lib/db";

beforeEach(async () => {
  // Clean test data between tests — tables will exist after Task 2
  // For now, this is a placeholder that imports db to verify the connection
});
